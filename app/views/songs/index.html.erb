<header>
  <p><%= params.fetch(:artist) %></p>
  <p><%= params.fetch(:song) %></p>
</header>

<ul class="parts">
  <%= render(@parts) || render("empty") %>
</ul>

<%= render "identifications/form", identification: Identification.new %>
